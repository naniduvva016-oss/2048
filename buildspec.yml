[200~version: 0.2

phases:
    build:
          commands:
                  - echo "Build Started"
                        - docker build -t 2048-game .
                              - echo "Build Completed"
